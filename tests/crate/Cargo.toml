[package]
name = "miniserde_test"
version = "0.0.0"
authors = ["David Tolnay <dtolnay@gmail.com>"]
edition = "2021"
publish = false

[lib]
path = "test.rs"

[dependencies]
miniserde = { path = "../..", default-features = false }

[features]
default = ["std"]
std = ["miniserde/std"]
