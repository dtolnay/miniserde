language: rust

rust:
  - nightly
  - stable
  - beta
  - 1.26.0

matrix:
  include:
    - rust: nightly
      env: MINIMAL
      script:
        - cargo update -Z minimal-versions
        - cargo check
